package com.example.project.musicvoter

import android.content.Context
import android.widget.TextView

/**
 * This class is used in `activity_room` mainly to draw the Title text
 */
class TitleTextView(context: Context): TextView(context) {
}